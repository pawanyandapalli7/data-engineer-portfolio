CREATE TABLE claims (
    claim_id        STRING,
    member_id       STRING,
    claim_amount    DECIMAL(10,2),
    claim_date      DATE,
    status          STRING,
    updated_at      TIMESTAMP
);
